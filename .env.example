# Copy to .env and fill in (never commit .env -- it is gitignored)
OPENAI_API_KEY=
# Optional second voter for the fusion cascade:
GEMINI_API_KEY=
# GEMINI_ENABLE=1
# Optional model overrides (defaults shown):
# OPENAI_MODEL=gpt-5-mini
# GEMINI_MODEL=gemini-flash-latest
# Optional quality gate (default 0.8):
# LLM_MIN_SUCCESS=0.8
# Optional throughput (default 8):
# AGENT_WORKERS=12
